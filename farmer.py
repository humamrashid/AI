#!/usr/bin/python3

# Implementation of farmer, fox, goose and grain problem solution.

# parameters: tuple(location, status)
# returns: string(action)
def reflex_farmer_agent(locstat):
    print(locstat[0], locstat[1])

print(reflex_farmer_agent(("A", 1)))

# EOF.
